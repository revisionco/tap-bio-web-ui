import styled from 'styled-components';

export { styled, css, keyframes, injectGlobal, ThemeProvider, withTheme, ServerStyleSheet, StyleSheetManager } from 'styled-components';

export default styled;